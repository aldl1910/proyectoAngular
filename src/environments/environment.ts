// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
	  apiKey: "AIzaSyB3GyRtXm6I-F63oAB0NWf_72HbP2vIbgI",
	  authDomain: "aplicacionweb-4c3f0.firebaseapp.com",
	  projectId: "aplicacionweb-4c3f0",
	  storageBucket: "aplicacionweb-4c3f0.appspot.com",
	  messagingSenderId: "688126615550",
	  appId: "1:688126615550:web:797c668da17ac3562faaaf"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
